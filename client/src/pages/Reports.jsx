import {useEffect,useState} from 'react';import {api} from '../api';
export default function Reports(){const [d,setD]=useState(null),[err,setErr]=useState('');useEffect(()=>{api('/reports/summary').then(setD).catch(e=>setErr(e.message))},[]);const exportCSV=()=>{if(!d)return;const csv=Object.entries(d).map(([k,v])=>`"${k.replace('_',' ')}","${v}"`).join('\n');const blob=new Blob([`"Metric","Value"\n${csv}`],{type:'text/csv'});const url=window.URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='village_report.csv';a.click();};return <><div className="page-title row"><div><h1>Reports & Analytics</h1><p>Live summary generated from MongoDB collections</p></div><button onClick={exportCSV}>Export to CSV</button></div>{err&&<div className="error">{err}</div>}<div className="stats">{d&&Object.entries(d).map(([k,v])=> <div className="stat" key={k}><span>{k.replace('_',' ')}</span><strong>{v}</strong></div>)}</div><div className="panel"><h2>Available Reports</h2><p>Demographics, service status, welfare applications, certificates and resource utilization are available through protected Express API endpoints.</p></div></>}
