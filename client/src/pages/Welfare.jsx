import {useEffect,useState} from 'react';import {api} from '../api';export default function Welfare(){const [rows,setRows]=useState([]);useEffect(()=>{api('/welfare/schemes').then(setRows)},[]);return <><div className="page-title"><h1>Welfare Schemes</h1><p>Government benefits available to village citizens</p></div><div className="cards">{rows.map(x=><div className="panel" key={x._id}><h3>{x.name}</h3><p>{x.description}</p><small>Eligibility: {x.eligibility}</small></div>)}{!rows.length&&<div className="panel">No schemes added yet.</div>}</div></>}
