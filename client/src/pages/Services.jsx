import {useEffect,useState} from 'react';import {api} from '../api';import {useAuth} from '../context/AuthContext';export default function Services(){const {user}=useAuth();const [rows,setRows]=useState([]),[f,setF]=useState({title:'',category:'Water Supply',description:''});const load=()=>{api('/services/requests').then(setRows);};useEffect(load,[]);const save=async e=>{e.preventDefault();await api('/services/requests',{method:'POST',body:JSON.stringify(f)});load()};const updateStatus=async(id,status)=>{await api(`/services/requests/${id}`,{method:'PUT',body:JSON.stringify({status})});load()};return <><div className="page-title"><h1>Service Requests</h1><p>Submit and track village service requests</p></div><form className="panel form-grid" onSubmit={save}><input placeholder="Request title" onChange={e=>setF({...f,title:e.target.value})}/><select onChange={e=>setF({...f,category:e.target.value})}>{['Water Supply','Road Repair','Sanitation','Electricity','Other'].map(x=><option key={x}>{x}</option>)}</select><input placeholder="Description" onChange={e=>setF({...f,description:e.target.value})}/><button>Submit Request</button></form><div className="cards">{rows.map(x=><div className="panel" key={x._id}><h3>{x.title}</h3><p>{x.category} · <b>{x.status}</b></p><p>{x.description}</p>{user?.role==='admin'&&<select value={x.status} onChange={e=>updateStatus(x._id,e.target.value)} style={{marginTop:'10px',padding:'6px'}}><option>Pending</option><option>In Progress</option><option>Resolved</option><option>Rejected</option></select>}</div>)}</div></>}
