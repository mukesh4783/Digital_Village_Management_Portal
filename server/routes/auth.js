import {Router} from 'express'; import bcrypt from 'bcryptjs'; import jwt from 'jsonwebtoken'; import User from '../models/User.js';
const r=Router(); const token=u=>jwt.sign({id:u._id,role:u.role,name:u.name},process.env.JWT_SECRET,{expiresIn:'7d'});
r.post('/register',async(req,res)=>{try{if(await User.findOne({email:req.body.email})) return res.status(400).json({error:'Email already registered'});const u=await User.create({...req.body,password:await bcrypt.hash(req.body.password,10)});res.status(201).json({token:token(u),user:{id:u._id,name:u.name,email:u.email,role:u.role}});}catch(e){res.status(400).json({error:e.message});}});
r.post('/login',async(req,res)=>{const u=await User.findOne({email:req.body.email});if(!u||!await bcrypt.compare(req.body.password,u.password)) return res.status(401).json({error:'Invalid credentials'});res.json({token:token(u),user:{id:u._id,name:u.name,email:u.email,role:u.role}});});
r.get('/me',async(req,res)=>{try{const p=req.headers.authorization?.split(' ')[1];const d=jwt.verify(p,process.env.JWT_SECRET);const u=await User.findById(d.id).select('-password');res.json(u);}catch{res.status(401).json({error:'Authentication required'});}}); r.post('/logout',(req,res)=>res.json({message:'Logged out'})); export default r;
