import {Router} from 'express'; import {auth,admin} from '../middleware/auth.js';
export const crudRouter=(Model,{adminWrite=false}={})=>{const r=Router();r.use(auth);r.get('/',async(req,res)=>res.json(await Model.find().sort({createdAt:-1})));r.get('/:id',async(req,res)=>{const x=await Model.findById(req.params.id);x?res.json(x):res.status(404).json({error:'Not found'});});r.post('/',adminWrite?admin:(req,res,next)=>next(),async(req,res)=>{try{res.status(201).json(await Model.create(req.body));}catch(e){res.status(400).json({error:e.message});}});r.put('/:id',adminWrite?admin:(req,res,next)=>next(),async(req,res)=>res.json(await Model.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true})));r.delete('/:id',admin,async(req,res)=>{await Model.findByIdAndDelete(req.params.id);res.json({message:'Deleted'});});return r;};
