import {Router} from 'express';import User from '../models/User.js';import Audit from '../models/Audit.js';import {auth,admin} from '../middleware/auth.js';const r=Router();r.use(auth,admin);r.get('/users',async(q,s)=>s.json(await User.find().select('-password')));r.get('/audit-log',async(q,s)=>s.json(await Audit.find().sort({createdAt:-1})));r.get('/settings',(q,s)=>s.json({villageName:'Digital Village',language:'English'}));r.put('/settings',(q,s)=>s.json(q.body));export default r;
