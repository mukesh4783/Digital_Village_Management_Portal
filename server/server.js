import 'dotenv/config';import express from 'express';import cors from 'cors';import {connectDB} from './config/db.js';import authRoutes from './routes/auth.js';import dashboard from './routes/dashboard.js';import services from './routes/services.js';import welfare from './routes/welfare.js';import notifications from './routes/notifications.js';import reports from './routes/reports.js';import adminRoutes from './routes/admin.js';import {crudRouter} from './routes/crud.js';import Citizen from './models/Citizen.js';import Household from './models/Household.js';import Certificate from './models/Certificate.js';import Resource from './models/Resource.js';
const app=express();app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173'}));app.use(express.json());app.get('/api/health',(q,s)=>s.json({status:'ok',stack:'MERN'}));app.use('/api/auth',authRoutes);app.use('/api/dashboard',dashboard);app.use('/api/citizens',crudRouter(Citizen,{adminWrite:true}));app.use('/api/households',crudRouter(Household,{adminWrite:true}));app.use('/api/services',services);app.use('/api/welfare',welfare);app.use('/api/certificates',crudRouter(Certificate));app.use('/api/notifications',notifications);app.use('/api/resources',crudRouter(Resource,{adminWrite:true}));app.use('/api/reports',reports);app.use('/api/admin',adminRoutes);app.use((e,q,s,n)=>s.status(500).json({error:e.message}));const port=process.env.PORT||5000;connectDB().then(()=>app.listen(port,()=>console.log(`Server running on http://localhost:${port}`))).catch(e=>{console.error(e);process.exit(1)});
